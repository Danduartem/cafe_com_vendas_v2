---
permalink: /thank-you/
layout: layout.njk
title: "Obrigada! Falta 1 passo"
description: "Página de confirmação do pagamento - próximos passos para o evento Café com Vendas"
---

{# Render sections using dedicated thank-you page loader #}
{% for section in thankYou.sections %}
  {% if section.enabled %}
    {# Section: {{ section.slug }}{% if section.variant %} ({{ section.variant }}){% endif %} #}
    {# Set section data for template access #}
    {% set currentSection = section.data %}
    {% if section.slug == "thank-you-content" %}
      {% include "sections/thank-you/index.njk" %}
    {% endif %}
  {% endif %}
{% endfor %}